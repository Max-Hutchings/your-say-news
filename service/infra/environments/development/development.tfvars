# Non-secret development values only. Replace the reserved example domains after the real
# Cloudflare zone and API hostname are approved.
api_hostname         = "api.development.example.invalid"
cloudflare_zone_name = "example.invalid"
aiven_project_name   = "your-say-news"
