package com.yoursay.posts;

/**
 * The kind of media attached to a post. Crosses the domain boundary (DTOs), so it lives at the
 * domain's public top level.
 */
public enum MediaType {
    IMAGE,
    VIDEO
}
