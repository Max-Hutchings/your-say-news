package com.yoursay.user.usercharacteristic.dto;

/** One ordered, user-facing option for an enum-backed characteristic answer. */
public record CharacteristicOptionDto(String label, String value) {
}
