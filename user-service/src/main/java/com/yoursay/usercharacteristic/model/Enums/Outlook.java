package com.yoursay.usercharacteristic.model.Enums;

/** A user's disposition toward the future — a non-identifying outlook bucket. */
public enum Outlook {
    OPTIMIST,
    PESSIMIST,
    DEPENDS
}
