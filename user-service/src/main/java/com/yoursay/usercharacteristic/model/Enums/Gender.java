package com.yoursay.usercharacteristic.model.Enums;

/** Self-identified gender. {@link #SELF_DESCRIBE} pairs with the free-text genderSelfDescribe field. */
public enum Gender {
    WOMAN,
    MAN,
    NON_BINARY,
    SELF_DESCRIBE,
    PREFER_NOT_TO_SAY
}
